<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/background_color">

    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:id="@+id/bottomSheetDragHandle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <SearchView
        android:id="@+id/habitsSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:iconifiedByDefault="false"
        app:layout_constraintStart_toStartOf="@id/bottomSheetDragHandle"
        app:layout_constraintTop_toBottomOf="@id/bottomSheetDragHandle"
        app:queryHint="Найти привычку" />

    <TextView
        android:id="@+id/priorityFilterTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/priority_title"
        app:layout_constraintStart_toStartOf="@id/habitsSearch"
        app:layout_constraintTop_toBottomOf="@id/habitsSearch" />

    <Spinner
        android:id="@+id/habitPriorityFilter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:entries="@array/types_array"
        android:popupBackground="@color/background_color"
        app:layout_constraintStart_toStartOf="@id/priorityFilterTitle"
        app:layout_constraintTop_toBottomOf="@id/priorityFilterTitle" />

    <Button
        android:id="@+id/btnSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/habitPriorityFilter"
        app:layout_constraintTop_toBottomOf="@id/habitPriorityFilter"
        android:layout_marginTop="8dp"
        android:text="Найти"
        />
</androidx.constraintlayout.widget.ConstraintLayout>